import React from 'react';

const Toolbar: React.FC = () => {
  return (
    <div>
      <h2>Toolbar</h2>
      {/* Instrument selection, tempo, export, save/load */}
    </div>
  );
};

export default Toolbar;